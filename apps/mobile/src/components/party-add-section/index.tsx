import { StyledScrollDiv } from "@features/user/id";

export const SectionPartyAdd = ({ children }: any) => {
  return (
    <StyledScrollDiv className={`mx-[22px] flex-1`}>{children}</StyledScrollDiv>
  );
};
