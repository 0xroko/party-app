import { styled } from "nativewind";
import { Text as T, TextInput, View } from "react-native";

export const Div = styled(View);
export const Text = styled(T);
export const StyledInput = styled(TextInput);
